FROM node:20-alpine
RUN apk add --no-cache git docker-cli docker-compose bash
WORKDIR /app
COPY webhook-server.cjs ./
COPY deploy-docker.sh ./deploy.sh
RUN chmod +x deploy.sh
EXPOSE 9000
ENV LISTEN_HOST=0.0.0.0
CMD ["node", "webhook-server.cjs"]
